# DisasterLink Frontend (React + Vite + Tailwind + Leaflet)

## Quick start
1. npm install
2. npm run dev

This project uses a mock localStorage-based API for reports. Replace with your backend as needed.
